apps:
  # CLI
  - fastfetch
  - fzf
  - ripgrep
  - starship
  - usql
  - { brew: uv }

  # Runtime manager
  - { brew: asdf }

  # Editor/terminal
  - { brew: neovim, apt: neovim, pacman: neovim }
  - { brew: wezterm }
  - { brew: tmux, apt: tmux, pacman: tmux }

  # Desktop / WM (macOS)
  - { brew: raycast }
  - { brew: nikitabobko/tap/aerospace }

  # Docker (simple)
  - { brew: docker, apt: docker.io, pacman: docker }

  # Fonts (macOS via cask-fonts)
  - { brew: font-recursive-mono-nerd-font }
  - { brew: font-jetbrains-mono-nerd-font }
  - { brew: font-fira-code-nerd-font }
  - { brew: font-symbols-only-nerd-font }
